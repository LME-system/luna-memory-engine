# 待分析命题：事件相机 × 数字神经突触阵列 × SNN 大模型

## 背景（用户原文）
事件相机（Event Camera）是一种仿照生物视网膜工作原理的神经形态视觉传感器，与传统相机的成像方式有本质区别。传统相机通过快门一次性捕捉整帧图像，而事件相机不会输出完整帧，而是让每个像素独立监测亮度变化，当某个像素的亮度变化超过设定阈值时，立即发出一个"事件"（包含位置、时间戳和亮度变化方向）。没有变化的像素不产生任何数据，输出是异步的事件流，而非固定频率的整帧画面。

## 核心命题
**事件相机的处理器可以设计为数字神经突触阵列，用于计算和短期记忆（权重），形成一种 SNN（脉冲神经网络）的大模型形态么？**

## 要求
评估该命题的：可行性、必要条件、边界/不可行点、与现有技术（存算一体/忆阻器/数字 SRAM 突触/SNN 加速器/事件相机现有 SoC）的关系、以及"SNN 大模型形态"这一说法是否成立。
