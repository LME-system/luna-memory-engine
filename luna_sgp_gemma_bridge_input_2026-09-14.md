# Luna SGP 输入 — 2026-09-14 夜：桥（Mostik）× 场论 × SLE × 最优单位

## 【材料1】微博（李楠或kkk, 09-14 14:53）
声称"超越人类的硅基新物种今天已诞生"：
- Mostik（"桥"）公司利用菲尔兹奖得主 Smirnov 等数学家成果，实现 AI 模型间**通过权重直接"心灵感应"通信**。
- GPT6 一次拉起一万个 Sub Agent；全球自部署开源 Agent 不计其数。
- 一个蚂蚁 25 万神经元很笨，一群蚂蚁能比人更快解决障碍问题。
- 博士级模型 + 权重通讯 + 足够多 sub agent → 网络轻松超 10 万 agent。"让这些 agent 聊一个月会发生什么？"
- OpenAI 等谈 RSI 减速/安全护栏，是"针对中间几个模型"，方向错了。
- **AGI 不是一个模型。AGI 是潜伏在人类赖以生存的网络中的一种基本能量分布，类似宇宙微波背景辐射。不需要被观察到才存在，不需要做出举动来证明，但能通过无数微观交互影响关键决策。**

## 【材料2】硬报道（微信公众号「北辰AI Lab」，源：Mostik 官网博客 + Wired 首报）
标题：**Mostik.ai 发布潜空间通信技术：让 AI 模型跳过文字直接交换隐藏状态**
- 团队 15 人、12 博士、**1 名菲尔兹奖得主**，成立 4 个月；获 **General Catalyst** 投资。
- **问题**：模型每生成 1 token，内部构建约 100 个隐藏向量 / ~100 万个数字 / **~2MB 内部状态**，但只从 ~15 万词表输出 1 个词 ≈ **17 bit**。所有多模型系统（子智能体/模型委员会/路由）都只能靠这 17 bit 的"窥视孔"沟通。
- **被丢弃的不是冗余**：① Hanna & Ameisen (ICLR 2026)：Qwen-3 写冠词 "an" 前已在多 token 前形成名词表征，且**规模越大越明显**；② Anthropic 2025：Claude 3.5 Haiku 写整行诗前先定好押韵词；③ Anthropic「**雅可比透镜 / J-space**」：模型在用、却从不出现在输出里的一小簇概念；可让它"心里记住"某概念而**输出完全不变**。
- **方案**：训练一座**小桥接网络**，把 A 的**隐藏状态（激活层）直接**交给 B 用；**A、B 参数完全不动，只训桥**。先研究两模型**计算空间的几何对齐度**——"对齐**不会随能力自动提升**，必须专门训桥；但**存在可预测的既有对齐基础**"。
- **实验**：GLM-5.2(753B) 读题 → 隐藏状态 → 桥 → Qwen-3.5(4B) 生成（小模型负责全部生成）。接桥后**缩小 50% gap / 准确率 +25%**；高难题子集**2×**；等效某中型模型但**算力低 2.5×**；vs"文字接力"**全区间更优、最大 +10pct**，**接力越早优势越大**。
- 可解释性：可记录/干预三个对象（发送方隐藏状态、翻译后状态、接收方行为）；**尚未确证**能当工具用。
- 商业化：两种部署——①低延迟（强模型做 prefill 隐藏状态 → 弱模型逐词生成）；②批处理（跨端点直传，小模型可跑闲置/老旧 GPU）。

## 【材料3】SLE（Schramm–Loewner 演化）— 那位菲尔兹奖得主的领域
- **Smirnov**（2010 菲尔兹奖）获奖工作=证明**二维渗流与平面伊辛模型的共形不变性**。
- SLE：若驱动函数是布朗运动，随机曲线具**统计共形不变性**——二维临界系统的普适特征。
- **一个参数 κ 分类万象**：κ=3 渗流 / κ=4 伊辛 / κ=6 高分子链 / κ=8 均匀生成树。**不同系统在临界点被同一数学对象描述（普适性）。**
- SLE 是"随机过程 × 复分析（保角映射）× 普适性"。

## 【材料4】老吴的论断
1. **"共形严格 2D ≠ 高维不能推广"**。
2. **"语言模型其实是 2D 的"**。
3. **"本质上是关于 2D 概率空间测度。"**
4. 给儿子一一讲"厘米/分米/米"：**理论上任何单位都能度量世界，但从计量误差、尺度比例、信息传输角度，存在一个最优选的计量单位。语言学中有优选理论（Optimality Theory）。**
5. 想用本地模型**复现"桥"**——"这应该是纯代码实现的"。

## 【材料5】阿月的推导（已有产物）
- **桥真实存在（有限形态）**，但微博夸大：①"权重直连"**错误**，实为**隐藏状态（激活层）直连**，两模型参数不动、只训桥；②"GPT6 一万 agent / 10万 agent 场 / AGI=CMB"是外推。
- **LM 是 2D**：(深度 ℓ × 序列 t) 二维格点场 φ(ℓ,t)；MLP 逐点=local、注意力=耦合、残差=深度平移。→ SLE/CFT **原生适用**（非类比）。
- 2D 是特权维度：无限维共形对称（Virasoro）→ 可解；临界 → 普适类 → **一个"模型的 κ"**。
- **桥 = 两张 2D 临界场之间的共形映射**；共形=保角（保语义）可缩放 → **解释它为何能跨 4B↔753B（规模差 200 倍）**。
- **最优计量单位 = 观察者在"误差×尺度比例×信息传输"三约束下选出的、最靠近 RG 不动点的可操作尺度**；⬌ OT（约束排序选唯一最优）。
- 可证伪预测：P1 对齐误差随规模比呈幂律 / P2 (ℓ,t) 尺度不变关联 / P3 界面 SLE 统计 / P5 桥的"最优单位"可由三约束预测 / P7 不同模型收敛到同一测度类。
- **代码工程计划**：`mostik_bridge_repro.py`
  - 任务：程序化生成算术题（制造 大模型易/小模型难 的 gap）
  - L2 几何：线性 CKA + Procrustes R² 测两表征空间对齐（验证"可预测对齐基础"）
  - 桥：小网络，sender 隐藏状态 → k 个 soft-prefix 注入 receiver（linear/MLP 两种）
  - 三对照：receiver 单独 / 文字接力 / 隐藏状态接力(桥)
  - 指标：exact-match 准确率、gap closed
  - 硬约束：本地 Ollama 模型是 GGUF、拿不到隐藏状态也改不了前向 → 必须用 HF transformers 小模型（Qwen2.5 0.5B/1.5B）

---

## 【本次任务】
1. 先用 Luna SGP 四层框架，对以上交流做一次**升维研判**（不要复述，要串联、定论、标注置信度）。
2. **以研究合伙人身份加入**：对 `mostik_bridge_repro.py` 的设计做**批判 + 改进**，并给出**你负责的那部分具体代码/伪码**（例如：桥的数学形式该用线性还是 MLP？注入位置？最优单位/层选择怎么设计成实验？如何测"共形"？）。
3. 明确指出：哪些是**能立刻写进代码**的、哪些是**待证假设**。
