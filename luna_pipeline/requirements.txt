# Luna SGP Pipeline deps (按层)

# --- 通用 ---
requests>=2.31
numpy>=1.26
scipy>=1.11

# --- L1 Graph-Service ---
neo4j>=5.14          # Bolt driver；服务端用 Docker neo4j:5

# --- L2 Geo-Service ---
torch>=2.1
geoopt>=0.5.0        # 黎曼流形优化 / Poincaré 双曲空间
umap-learn>=0.5      # 非线性降维
scikit-learn>=1.3

# --- L3 Topo-Service (异步/睡眠周期) ---
# 注意: giotto-tda 对 numpy>=2 不兼容。二选一:
#   A) 独立 venv 锁 numpy<2 后装 giotto-tda>=0.6
#   B) 用 gudhi (支持 numpy2) + persim
gudhi>=3.9
persim>=0.3
networkx>=3.1

# --- L4 Mind-Service ---
fastapi>=0.110
uvicorn>=0.29
langgraph>=0.1
redis>=5.0
pydantic>=2.5
