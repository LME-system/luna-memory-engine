# 聚焦命题：仅从【计算单元】角度看可行性

## 范围限定（重要）
**只看"计算单元"这一层**。即：把事件相机处理器里的"数字神经突触阵列"当成一个**计算+短期记忆（权重）单元**来评估。
- 上游（事件相机传感器本身）与下游（系统集成、云端协同、传感器尺寸约束）**暂时排除**。
- 问题只问：作为计算单元，它能不能成立？效率/能耗/时延上成立的条件是什么？边界在哪？

## 待评估的对象
一个数字神经突触阵列单元，承担：
1. **突触权重存储**（长期记忆）
2. **事件驱动的乘累加/膜电位积分**（计算）
3. **短期记忆**（膜电位/适应电流等局部状态）

输入是异步事件流（AER: 地址+时间戳），输出是脉冲。

## 要求
- 与主流计算单元对比：数字 SRAM MAC 阵列（脉动/张量核）、近存计算(NMC)、存算一体(CIM/忆阻器 PCM/RRAM)、纯事件驱动数字单元(AER+LIF)。
- 给出：该计算单元成立的**充要条件**、能耗/面积/时延的**量级关系**、在哪一点上从"可行"翻转为"不可行"。
- 明确区分"作为计算单元可行"与"作为大模型可行"——后者不在本命题范围。
