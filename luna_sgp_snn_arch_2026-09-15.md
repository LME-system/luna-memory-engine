# 🌙 Luna SGP 四层分析 — 事件相机 × 数字神经突触阵列 × 弥漫连接架构
> 引擎: DeepSeek Flash (阿月) | 2026-09-15 | 命题：事件相机处理器能否构成动态长期记忆的 SNN 形态

## 0. 命题演化链（供后续对照）
事件相机(AER异步事件流) → 处理器设计为**线性变化阈值的神经阵列** → 空间等差阈值(温度计码)= **线吸引子/状态空间记忆** → **多类膜电位阈值子阵列 + 互连**(多时间尺度 τ 谱) → **弥漫性(体积传递)控制面**(低维广播调制) → **软编码 + 总线集成**后运算 = 现实路径。

---

## L1 符号层 (Symbolic)
- **事件相机**: [已验证工程]。本质 Δ强度→AER(地址事件表示)。解决了采样冗余，压力后移至异步处理。
- **数字神经突触阵列**: [已验证工程]。SRAM+本地逻辑；事件稀疏时 MAC 退化为**条件加法**。
- **SNN / LIF**: [理论成熟，工程部分验证]。非可微→靠 ANN-to-SNN 转换或替代梯度。
- **温度计码(等差阈值)**: [已验证工程]。= 模拟量→一元码量化器；**Δ = 记忆的计量单位**。
- **弥漫连接(体积传递/场耦合)**: [生物事实 → 硬件待证]。
- **"SNN 大模型形态"**: [宣称/假设]。**术语滥用重灾区**。

**信号强度**: 🔴 多时间尺度子阵列（架构关键）｜🟡 弥漫控制面（便宜但粗）｜🟢 "大模型"标签（应剥离）

## L2 几何层 (Geometric)
- **数据流**：异步 → 稀疏 → 低时延；算力 ∝ **场景动态度**，非分辨率。
- **记忆向量**：视界从**单一 τ** → **τ 的谱**（多类子阵列）。**类间 τ 比 = 设计自由度**。
- **能效向量**：瓶颈从"权重搬运能耗" → **"总线带宽/仲裁延迟"**。
- **拐点**：
  1. **时间尺度分离**：慢类须比快类慢 ≥1 个数量级，否则谱塌为一点。
  2. **总线饱和**：匹配稀疏度下降 → 总线占用率非线性上升 → 能效翻转。
  3. **增益/工作点**：递归+梯度阈值必须锁在临界附近，否则跑飞或全灭。

## L3 拓扑层 (Topological)
- **三层结构**：①多 τ 子阵列(时间谱) ②稀疏数据面(点对点/可寻址，传模式) ③弥漫控制面(广播/标量，调工作点+塑性闸门)。
- **闭环**：快子阵列 → 慢子阵列(巩固) —— 慢 → 快(上下文偏置)；弥漫信号 → 三因子塑性闸门。
- **瓶颈迁移链**：权重搬运 → 稠密布线(O(N²)) → **总线带宽**。
- **结构同构**：弥漫通道=带标签广播；子阵列类=区域 tag；数据面=地址事件。**一套 NoC，两种语义**。
- **与「桥/变速器」同构**：连接 = 码空间匹配；桥 = 可学的跨码空间匹配器。

## L4 编排层 (Orchestration)
- **统一主矛盾**：**"可学的码(连接自由度)" ⟷ "共享总线带宽"** 的对偶——你要的连接越多，总线越撑不住；能效旋钮在**码的稀疏度与可分离性**。
- **非线性触发点**：**码一经可学/可在线更新**，架构从"固定储层"相变到"可训练系统"——即从传感前端变成可塑记忆体。
- **认知错误（行业）**：把"类脑"当**容量竞赛**（忆阻器/参数规模）；真问题在**控制面与码设计**，不在容量。
- **追踪变量**：①匹配稀疏度 ②总线占用率/扇出 ③类间 τ 比 ④增益稳定裕度 ⑤控制面维度。
- **裁定**：作为**动态状态记忆+计算单元** = 有条件成立（前提：时间尺度分离、总线墙压住、码稀疏可学）。作为"LLM 式模式库" = 不成立（需慢塑性）。
- **置信度**：架构可行 80% / 总线墙为真瓶颈 75% / 码学习是相变点 60%。

---

## 交给 gemma 的研究任务
1. 批判上述 L1–L4：哪条判断被高估/低估？给出反例或更锋利的量。
2. **数字实现**：把"多 τ 子阵列 + 稀疏数据面 + 弥漫控制面 + 软编码总线"落成**具体电路/协议级设计**（阈值类如何设定 τ、tag 如何编、广播如何调制、匹配如何稀疏）。
3. **可证伪实验**：设计能区分"真神经形态"与"普通数字加速器"的最小实验，给出判据与阈值。
4. 标出：能立刻写进代码/硬件的 vs 待证假设。
